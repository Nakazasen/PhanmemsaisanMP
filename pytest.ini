[pytest]
markers =
    unit: fast isolated test with synthetic or in-memory inputs
    integration: test spanning multiple application layers without private raw Excel
    performance: opt-in benchmark or performance-budget test
    requires_raw_excel: tests that require private/local raw Excel workbooks under raw/
    real_pipeline_acceptance: explicit heavy gate that invokes the real CLI with private/local inputs
