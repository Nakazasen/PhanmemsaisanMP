# Phase 42N2B - Invariant Gap Accounting

## Classification

`PASS_PHASE_42N2B_INVARIANT_GAP_ACCOUNTING_READY`

## Scope

Rebuilt the old 136-row matrix using invariant identity keys first: account code,
business group/source family, F:Q month value/formula vector, formula/value
pattern, known business aliases, and description tokens only as weak support.

No output code was changed. No full pytest was run.

## Summary

| Metric | Count |
|---|---:|
| Old matrix rows | 136 |
| False gaps found | 2 |
| True missing source unconfirmed | 65 |
| Ambiguous / human mapping needed | 59 |
| Template-only not source | 10 |
| Implementable now | 0 |

## Hard checks

| Row | Description | Result |
|---:|---|---|
| 159 | 電気代 | matched to generated Facility row 204 `electricity` |
| 160 | 水道代 | matched to generated Facility row 205 `water` |

## Output CSV

`docs/audits/phase42n2b_invariant_gap_accounting.csv`

## Method correction

The prior matrix was useful as a row trace, but it treated some rows as missing
because description/fuzzy matching was too weak. Invariant accounting correctly
marks `電気代` and `水道代` as already generated by the Facility writer because
their business meaning and month vectors match `electricity` and `water`.

## Top next candidate group

The largest remaining candidate remains the fixed-assets/detail family around
account `5005026371`. It is not implementable yet because it needs exact source
row/cell and Apr-Mar value mapping, not fuzzy description hits.

## Conclusion

`PASS_PHASE_42N2B_INVARIANT_GAP_ACCOUNTING_READY`
