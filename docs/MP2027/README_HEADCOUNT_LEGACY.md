# Manual Headcount Legacy File

`headcount_manual_LEGACY_DO_NOT_USE.csv` is not an active input file.

It is kept only as a historical artifact. It may contain historical or
unconfirmed rows that must not be imported, copied, or treated as current
business data.

Active manual headcount input is:

`raw/headcount_manual.csv`

Use the GUI headcount editor or edit `raw/headcount_manual.csv` directly when
entering confirmed manual headcount data.
