# MP2027 Manager 0.1.9

Ngày phát hành: `2026-09-07` (Asia/Bangkok)

## Phạm vi

- Khắc phục triệt để lỗi thiếu quy định đồng phục FY2027 trên các máy cập nhật tự động qua mạng (Auto-update).
- Bổ sung cơ chế tự phục hồi (self-healing) hai tầng:
  - Tầng 1: Trong `ensure_fiscal_year(2027)`, nếu FY2027 đã tồn tại nhưng `uniform_policy` rỗng, tự động nạp đường dẫn tương đối chuẩn và lưu lại cấu hình.
  - Tầng 2: Trong `fiscal_paths(2027)`, khi `uniform` bị rỗng hoặc trỏ tới đường dẫn chết trên máy khác, tự động quét tìm file `raw/Cải tiến nhập dữ liệu chung vào file MPnew 10.07.2026.xlsx`, cập nhật `entry["uniform_policy"]` và tự động lưu `project.json`.
- Hỗ trợ tự động trích xuất file hạt giống từ `_internal/raw/` của bộ cài vào thư mục `%LOCALAPPDATA%` của người dùng nếu chưa có sẵn.
- Duy trì nguyên tắc Fail-Closed cho các đường dẫn tùy chỉnh do người dùng chỉ định rõ ràng.

## Kiểm tra trước build

- Source baseline: nhánh `main` đồng bộ `origin/main`.
- Catalog LAN trước publish: schema `1`, version `0.1.8`, package/hash/kích thước đã được đọc và đối chiếu khớp.
- Toàn bộ 80 test case tự động bao gồm bộ kiểm thử cập nhật, phân giải đường dẫn và tự phục hồi đều đạt (100% pass).

## Build, health-check và publish

- `py scripts/package_app.py`: thành công; build portable app, launcher và install bundle mới từ source hiện tại.
- Portable `--health-check` và launcher `--health-check`: đạt trong entrypoint đóng gói.
- Inno Setup `6.4.1`: biên dịch Setup thành công ra `MP2027_Manager_Setup_0.1.9.exe`.
- Package `.mpupdate` được kiểm tra tính toàn vẹn deterministic, hash check và dung lượng an toàn.

## Artifact và đối chiếu LAN

| Artifact | Kích thước | SHA-256 |
|---|---:|---|
| `release_artifacts/MP2027_Manager_Setup_0.1.9.exe` | 71,430,619 byte | `35999a53cd534c8ddfda1527d2b4dc0ff6e7e81a7ae37ed9b64806b6333486f2` |
| `release_artifacts/MP2027_Manager-0.1.9.mpupdate` | 86,293,309 byte | `be6682c0fd1d3da7dafb1faf6e0fa31f9cea7936355595e44a56653f0a3d0090` |

- Setup đã được copy qua tệp tạm `.part` vào thư mục phần mềm LAN, đối chiếu SHA-256 và kích thước khớp 100% trước khi đổi tên thành `.exe`.
- Package `.mpupdate` đã được phát hành nguyên tử lên `release_update`.
- Catalog `latest.json` được cập nhật sau cùng trỏ tới `0.1.9`, đúng hash và kích thước.
- Cả hai thư mục LAN đã được kiểm tra lại sau khi phát hành; không còn file `.part`.
- Hoàn tất phát hành: `2026-09-07 13:07:20 +07:00` (Asia/Bangkok).
