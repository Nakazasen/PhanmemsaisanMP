# MP2027 Manager 0.1.7

Ngày phát hành: `2026-09-03` (Asia/Bangkok)

## Phạm vi

- Bổ sung bộ chọn nguồn AI trong Trợ lý vận hành, cho phép dùng C-Agent (KDTVN
  AI) hoặc Gemini Web.
- Lưu lựa chọn nguồn AI theo người dùng và đưa C-Agent thành lựa chọn mặc định.
- Bổ sung client C-Agent cho hội thoại nghiệp vụ, gồm xử lý phản hồi JSON/Markdown
  và các trạng thái timeout/lỗi HTTP.
- Bổ sung bản dịch Việt, Nhật và Anh cùng các kiểm tra hồi quy cho luồng này.

## Kiểm tra trước build

- Source baseline: `71219594715192ae8106959e9dccc431202ee3a9` trên `main` đồng bộ
  `origin/main`; các thay đổi version/release note được giữ local, không tự
  commit/push.
- Catalog LAN trước publish: schema `1`, version `0.1.6`, package/hash/kích
  thước đã được đọc và đối chiếu khớp.
- Release gate bắt buộc cùng hồi quy C-Agent/i18n: `106 passed`.
- Kiểm tra UI chuyên biệt cho provider: `2 passed`.

## Build, health-check và publish

- `py scripts/package_app.py`: thành công; build portable app, launcher và
  install bundle mới từ source hiện tại.
- Portable `--health-check` và launcher `--health-check`: đạt trong entrypoint
  đóng gói.
- Inno Setup `6.4.1` (fallback được playbook ghi nhận): biên dịch Setup thành
  công. Các warning về key dịch không nhận diện không làm compiler thất bại;
  build/package test đã đạt.
- Package `.mpupdate` được kiểm bằng `inspect_update_package`: schema `1`, kind
  `application`, version `0.1.7`, min app version `0.1.1`, database schema `1`,
  `1,863` file.

## Artifact và đối chiếu LAN

| Artifact | Kích thước | SHA-256 |
|---|---:|---|
| `release_artifacts/MP2027_Manager_Setup_0.1.7.exe` | 71,425,318 byte | `ebfbadd388461088e1140b3a41f9eeac83843d7526817c5ccb2ff17bacfb995f` |
| `release_artifacts/MP2027_Manager-0.1.7.mpupdate` | 86,285,959 byte | `7e860ec24e5c368ea2bdb57078464817db311392e82b4fd486453c9e5aa51177` |

- Quyền ghi `release_update` đã được kiểm bằng probe tạm có tên duy nhất và
  probe tự được xóa trước publish.
- Setup đã copy bằng `.part` vào thư mục phần mềm LAN, đối chiếu hash/kích thước
  sau copy rồi đổi tên thành `.exe`; hash/kích thước LAN khớp local.
- Package đã publish atomically vào `release_update`; `latest.json` chỉ được
  thay sau package. Catalog hiện trỏ `0.1.7`, đúng package, kích thước và
  SHA-256 nêu trên.
- Cả hai thư mục LAN đã được đọc lại sau publish; không còn file `.part`.
- Hoàn tất publish: `2026-09-03 13:52:10 +07:00` (Asia/Bangkok).

## Pilot

- Chưa thực hiện trên máy pilot ở version thấp hơn qua GUI. Đây là bước theo dõi
  còn lại: xác nhận release notes, version `0.1.7`, dữ liệu runtime, backup
  `before-0.1.7` và rollback pointer. Không ghi nhận bước này là đã đạt khi chưa
  có máy pilot thực tế.

- Chính sách cập nhật: `HASH_ONLY_LAN`; không tạo hoặc sử dụng khóa ký,
  `manifest.sig`, `key_id` hay `trusted_signing_keys`.
