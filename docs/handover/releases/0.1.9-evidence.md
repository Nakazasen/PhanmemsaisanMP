# Bằng chứng phát hành MP2027 Manager 0.1.9

Ngày build: 23.07.2026.

## Artifact

| Artifact | Kích thước | SHA-256 |
|---|---:|---|
| `MP2027_Manager-0.1.9.mpupdate` | 82.685.340 byte | `dae669e60a61063d29deba453833ff18dac5bcef11b31f254bf215992fade98b` |
| `MP2027_Manager_Setup_0.1.9.exe` | 68.190.831 byte | `66c211130c02b06d04049219d1e5f0a9aa374dd3419ff7668a9f0e556e3fcb6e` |
| `docs/MP2027/FORM.xlsx` | 188.518 byte | `c4fb460adc2c5f017b914c913c2681fab387a88e9bdcd473fc1b7a5d354ff332` |

Hash local và bản trên nguồn LAN khớp tuyệt đối. Gói update được publish nguyên tử;
`latest.json` trỏ tới `0.1.9`, đúng tên package, kích thước và SHA-256. Setup được
copy vào thư mục `MP Saisan` trên LAN.

## Cổng kiểm thử

- Compile source: đạt.
- Regression trực tiếp cho FORM/output/account/event: `239 passed, 3 subtests passed`.
- Update delivery, application update và packaging entrypoint: `39 passed`.
- Cổng regression theo marker của playbook: `646 passed, 38 deselected, 6 subtests
  passed`; còn 7 test lỗi vì workspace không có bộ file headcount và Master Plan
  tham chiếu bên ngoài repo. Các test này chưa được gắn marker `requires_raw_excel`.
- Full suite: `654 passed, 2 skipped, 6 subtests passed`; 35 lỗi đều do thiếu các
  workbook raw Facility/GA/System/headcount/reference bên ngoài workspace.
- Build PyInstaller onedir, launcher, packaged resource validation và
  `--health-check`: đạt.
- Inno Setup 6.7.3: biên dịch thành công.

## Kiểm chứng FORM mới

- FORM runtime trong install bundle có cùng SHA-256 với FORM QLLN nguồn.
- Không còn `docs/MP2027/FORM_old.xlsx` ở source hoặc vị trí chính trong bundle;
  bản lịch sử dưới `docs/MP2027/old/` được giữ cho audit.
- Audit trực tiếp trên FORM mới xác nhận không có ô nào trong vùng dòng 30~37 bị
  thay đổi cả giá trị, công thức và style sau source-order pass.
- Account cố định tại dòng 31, 32, 33, 34 và 36 được giữ nguyên.
- Chi phí chung đầu tiên được ghi ở dòng 38; staging khấu hao Nhà/Đất được dọn.
- Không phát hiện token công thức hỏng `#REF!`, `#NAME?`, `#VALUE!`, `#DIV/0!`.

## Xác minh gói update

- Chữ ký Ed25519 xác minh thành công bằng public key `mp2027-prod-2026`.
- Manifest khai báo version `0.1.9`, entrypoint `MP2027_Portable.exe` và 1.859 file.
- FORM mới nằm trong payload đã ký.

## Giới hạn phát hành còn lại

- Setup có trạng thái Authenticode `NotSigned`; chữ ký Ed25519 của `.mpupdate`
  vẫn hợp lệ và là cơ chế tin cậy riêng của luồng update.
- Chưa thực hiện smoke trên profile Windows sạch thật và diễn tập rollback người
  dùng cuối trong lần build này.
