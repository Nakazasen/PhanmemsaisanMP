# Bằng chứng phát hành MP2027 Manager 0.1.7

Ngày build/publish: 21.07.2026
Kênh: `pilot`

## Artifact

| Artifact | Kích thước | SHA-256 |
|---|---:|---|
| `MP2027_Manager-0.1.7.mpupdate` | 82.649.588 byte | `b037e1014901f5acc6e9ec19db5435def0f6cf47e72f17d1fa08f32a9692cb96` |
| `MP2027_Manager_Setup_0.1.7.exe` | 68.153.504 byte | `64b89122a0edc92325fb13e556a3791c53d257a351b89ec0654373a8fba0065a` |

Cả hai artifact đã được copy vào nguồn LAN được cấu hình trong
`update_sources.default.json`; hash local và LAN khớp tuyệt đối. `latest.json`
trỏ tới `MP2027_Manager-0.1.7.mpupdate` với đúng version, kích thước, SHA-256 và
release notes.

## Cổng xác minh đã chạy

- `py -m compileall -q src scripts packaging`: đạt.
- Bộ test trực tiếp cho update, đóng gói, run history và source-order writer:
  `93 passed`.
- Regression không phụ thuộc raw Excel: `622 passed`, `7 failed`, `38 deselected`,
  `6 subtests passed`. Cả 7 lỗi do thiếu bộ file Excel tham chiếu bên ngoài tại
  môi trường build (6 lỗi nguồn headcount không có file, 1 lỗi thiếu Master Plan
  template), không phải assertion hồi quy của thay đổi 0.1.7.
- Gói update local và LAN đều vượt qua xác minh chữ ký Ed25519 bằng key id
  `mp2027-prod-2026`, manifest, hash, version và database schema.
- Không phát hiện private key, secret, `manual_inputs.db` hoặc run-history trong
  archive update.
- Diễn tập độc lập `stage_application_update` → health-check executable →
  `activate_staged_update` đạt; `current.json` kết thúc ở version `0.1.7` và đúng
  manifest SHA-256.

## Việc nghiệm thu còn lại

- Setup hiện chưa có chữ ký Authenticode (`NotSigned`). Chữ ký Ed25519 của
  `.mpupdate` vẫn hợp lệ và độc lập với Authenticode của Setup.
- Chưa thực hiện nghiệm thu GUI/cài mới và rollback N-1 → N trên một profile
  Windows sạch riêng biệt.
