# MP2027 Manager 0.1.5

Ngày phát hành: `2026-08-12 16:06` (Asia/Bangkok)

## Phạm vi

- Tiếp nhận toàn bộ thay đổi nghiệp vụ đã audit trước đó: cảnh báo FORM đúng
  điều kiện, bảo tồn `B31:D36`, công thức du lịch theo tháng 4, mũ người mới
  theo nhóm nhân sự của phòng `1412000019`, sắp xếp chi phí phúc lợi tháng 4 →
  tháng 3 và gom nhóm chi phí người mới.
- Khôi phục yêu cầu nhập baseline tháng 3 trước khi tính; cho phép chọn dữ liệu
  tháng 4 để điền baseline tháng 3.
- Chuẩn hóa playbook/handover theo `HASH_ONLY_LAN`; catalog LAN là nguồn sự thật
  duy nhất, không dùng artifact hoặc nhánh lịch sử để suy ra version.

## Kiểm tra

- Source baseline: `4fa2f7f7c2144d3a274a768dca6f474b4b661c45` (worktree có các
  thay đổi nghiệp vụ đã audit, chưa commit/push).
- Bộ kiểm thử phát hành bắt buộc: `68 passed`.
- Bộ hồi quy nghiệp vụ mục tiêu sau thay đổi tài liệu/version: `68 passed`.
- Portable và launcher `--health-check`: exit code `0`.
- Manifest package: schema `1`, version `0.1.5`, `1.859` file, không có đường
  dẫn không an toàn.
- Bộ test đọc nguồn `raw/FY2027` không nghiệm thu trên workspace này vì các file
  fixture 12/16/64/72 không tồn tại; cổng phát hành dùng bộ test bắt buộc và các
  fixture tổng hợp, không suy diễn dữ liệu thiếu.

## Artifact và đối chiếu LAN

| Artifact | Kích thước | SHA-256 |
|---|---:|---|
| `release_artifacts/MP2027_Manager_Setup_0.1.5.exe` | 69,586,806 byte | `48f23a3f01643a3d02ae3b6247868b0758c4b55a9881d0473021af1a20de3220` |
| `release_artifacts/MP2027_Manager-0.1.5.mpupdate` | 84,081,090 byte | `ef8889bb004ed8faa89fc131ee3a699a49a6a758b6101bb6c7b854519b3f9ff9` |

- Setup đã publish bằng `.part` vào thư mục phần mềm LAN; hash/kích thước local
  và LAN khớp, không còn `.part`.
- Package đã publish bằng `.part` vào `release_update`; hash/kích thước local,
  LAN và `latest.json` khớp, catalog trỏ version `0.1.5`, không còn `.part`.
- Không tạo hoặc sử dụng khóa ký, `manifest.sig` hay trường key.
- Chưa nghiệm thu thao tác cài update qua GUI trên máy pilot.
