# Bằng chứng phát hành MP2027 Manager 0.1.8

Ngày build: 21.07.2026.

## Artifact

| Artifact | Kích thước | SHA-256 |
|---|---:|---|
| `MP2027_Manager-0.1.8.mpupdate` | 82.653.452 byte | `0c95209819856630f3b980244367ef8ce765062d0b1c0c5b14d28b1c809e1e4a` |
| `MP2027_Manager_Setup_0.1.8.exe` | 68.159.422 byte | `cf3979ec490b1f1ab689e07fbe5eb485690f4b2a1f8bff842620d3b80dea116b` |

Hash local và bản trên nguồn LAN khớp tuyệt đối. `latest.json` được cập nhật sau
khi package đã publish nguyên tử và trỏ tới đúng version, package và SHA-256 nêu
trên.

## Cổng kiểm thử

- Compile source: đạt.
- Nhóm kiểm thử mục tiêu cho writer, đường xuất GUI, update delivery và tìm kiếm
  hướng dẫn: `61 passed`.
- Hồi quy chia hai nhóm: `358 passed` và `268 passed`.
- Bảy lỗi còn lại đều do dữ liệu tham chiếu ngoài môi trường build không có:
  sáu lỗi thiếu nguồn headcount và một lỗi thiếu Master Plan chính thức dùng làm
  template; không phải assertion của thay đổi 0.1.8.
- Build onedir và packaged health-check: đạt.
- Gói update được xác minh chữ ký Ed25519 bằng khóa công khai
  `mp2027-prod-2026`, không có đuôi file dữ liệu/source bị cấm, stage và activate
  độc lập từ `0.1.7` lên `0.1.8`: đạt.

## Kiểm chứng lỗi dòng 200

File cũ có ba payload phân bổ còn sót tại dòng 200–202 do pipeline truyền mốc dọn
cứng `199`. Writer mới xác định cuối vùng payload thực tế và dọn cả các công thức
template phía dưới. Bản kết quả hoàn chỉnh trong run history
`72e8a714a5144a76aa72bc322aad154c` có `0` ô dữ liệu A:T từ dòng 200 trở đi; các
mục trùng chỉ còn đúng một lần tại dòng 75, 78 và 79.

Việc publish lại file đó vào `OUTPUT_FY2027` bị Windows từ chối vì thư mục output
đang được một tiến trình khác giữ. Artifact trong run history vẫn hoàn chỉnh; cần
đóng Excel/tiến trình đang giữ thư mục rồi chạy lại để cập nhật output chính.

## Giới hạn phát hành còn lại

- Setup có trạng thái Authenticode `NotSigned`; chữ ký Ed25519 của `.mpupdate`
  vẫn hợp lệ và là cơ chế tin cậy riêng của luồng update.
- Chưa thực hiện smoke trên một profile Windows sạch thật và diễn tập rollback
  người dùng cuối.
