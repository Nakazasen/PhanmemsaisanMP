# MP2027 Manager 0.1.10

Ngày phát hành: `2026-09-07` (Asia/Bangkok)

## Phạm vi

- Thay thế và đóng gói tệp quy định đồng phục FY2027 bản mới nhất từ bộ phận nghiệp vụ (`D:\SETUP\tvn183660\download\Cải tiến nhập dữ liệu chung vào file MPnew 10.07.2026.xlsx` - dung lượng 6,570,406 byte, SHA-256 `4a2457a2c8211ab9dfaba77e70bb7d6d83fbbb3e24af0d02173945f695b0f192`) vào thư mục `raw/` của dự án và gói cài đặt.
- Nâng cấp cơ chế đồng bộ tệp hạt giống (`copy_missing_tree` trong `src/services/runtime_health.py`): tự động cập nhật đè tệp `Cải tiến nhập dữ liệu chung vào file MPnew 10.07.2026.xlsx` trong `%LOCALAPPDATA%` của người dùng khi kích thước tệp có sự khác biệt so với bản đóng gói. Điều này đảm bảo toàn bộ người dùng nâng cấp qua Auto-update đều được tự động đồng bộ tệp mới mà không bị file cũ cản trở.
- Cập nhật số phiên bản đồng bộ `0.1.10` trên toàn hệ thống (`release.json` và `installer/MP2027_Manager.iss`).

## Kiểm tra trước build

- Source baseline: nhánh `main` trên kho mã nguồn `MP2027`.
- Catalog LAN trước publish: schema `1`, version `0.1.9`, package/hash/kích thước đã được đọc và đối chiếu khớp 100%.
- Kiểm tra tính hợp lệ nghiệp vụ của tệp Excel mới: cấu trúc bảng, tên cột và dữ liệu quy định đồng phục đều hợp lệ (`is_uniform: True`).
- Bộ kiểm thử tự động đạt 100% pass (bao gồm kiểm thử cơ chế cập nhật đè khi kích thước thay đổi trong `test_copy_missing_tree_updates_uniform_policy_when_size_differs`).

## Build, health-check và publish

- `py scripts/package_app.py`: thành công; build portable app, launcher và install bundle mới từ source hiện tại.
- Portable `--health-check` và launcher `--health-check`: đạt trong entrypoint đóng gói.
- Inno Setup `6.4.1`: biên dịch Setup thành công ra `MP2027_Manager_Setup_0.1.10.exe`.
- Package `.mpupdate` được kiểm tra tính toàn vẹn deterministic, hash check và dung lượng an toàn.

## Artifact và đối chiếu LAN

| Artifact | Kích thước | SHA-256 |
|---|---:|---|
| `release_artifacts/MP2027_Manager_Setup_0.1.10.exe` | 73,680,613 byte | `27a8f5da25a93f58aac82d5453fa067daba313c8abe9f0a17f2b6f0be7da2754` |
| `release_artifacts/MP2027_Manager-0.1.10.mpupdate` | 88,541,935 byte | `a44bb053c3c76e4d0514fbb0711b181c75e0e92a8908bd6fe01ac9130bf217df` |

- Setup đã được copy qua tệp tạm `.part` vào thư mục phần mềm LAN, đối chiếu SHA-256 và kích thước khớp 100% trước khi đổi tên thành `.exe`.
- Package `.mpupdate` đã được phát hành nguyên tử lên `release_update`.
- Catalog `latest.json` được cập nhật sau cùng trỏ tới `0.1.10`, đúng hash và kích thước.
- Cả hai thư mục LAN đã được kiểm tra lại sau khi phát hành; không còn file `.part`.
- Hoàn tất phát hành: `2026-09-07 13:38:00 +07:00` (Asia/Bangkok).
