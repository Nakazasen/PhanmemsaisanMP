# MP2027 Manager 0.1.6

Ngày phát hành: `2026-08-12` (Asia/Bangkok)

## Phạm vi

- Chặn tính toán và xuất FORM khi người dùng chưa nhập baseline tháng 3 cho các
  Trung tâm chi phí thuộc phạm vi chạy.
- Không còn coi bản ghi `0/0/0` được sinh ra từ ba ô baseline để trống ở phiên
  bản cũ là dữ liệu hợp lệ.
- Vẫn chấp nhận baseline bằng 0 khi người dùng nhập/xác nhận rõ ràng, đồng thời
  giữ lựa chọn lấy dữ liệu tháng 4 làm baseline tháng 3 với provenance riêng.
- Áp dụng cùng một kiểm tra fail-closed ở giao diện và trong pipeline trước bước
  tính phân bổ, để không thể bỏ qua kiểm tra bằng đường chạy khác.

## Kiểm tra trước build

- Source baseline: `4fa2f7f7c2144d3a274a768dca6f474b4b661c45` (worktree chứa các thay đổi
  nghiệp vụ đã được người dùng yêu cầu, chưa commit/push).
- Release gate bắt buộc và test baseline: `91 passed`.
- Nhóm hồi quy baseline/GUI/pipeline/fiscal-run trước đó: `74 passed`.
- Kiểm tra mojibake: đạt.

## Build và health-check

- `py scripts/package_app.py`: thành công.
- Portable `--health-check`: exit code `0`.
- Launcher `--health-check`: exit code `0`.
- Inno Setup `6.7.3`: biên dịch Setup thành công.
- Manifest update: schema `1`, version `0.1.6`, minimum app version `0.1.1`,
  database schema `1`, `1.859` file; kiểm tra package bằng chính bộ cài đặt update
  của ứng dụng đạt.

## Artifact và đối chiếu LAN

| Artifact | Kích thước | SHA-256 |
|---|---:|---|
| `release_artifacts/MP2027_Manager_Setup_0.1.6.exe` | 69,588,387 byte | `d3cd226b705d1ab707781e55b5e5eafb8cfc4d1e2299287f20315c9e4ffd0700` |
| `release_artifacts/MP2027_Manager-0.1.6.mpupdate` | 84,082,684 byte | `226c4de8f62162d3abb22af60783ab6fe0a50ee5522d0da2b49a8b5cf5a79e17` |

- Setup đã publish bằng `.part` vào thư mục phần mềm LAN; hash/kích thước local
  và LAN khớp, không còn `.part`.
- Package đã publish nguyên tử vào `release_update`; hash/kích thước local, LAN
  và `latest.json` khớp. Catalog hiện trỏ version `0.1.6`, không còn `.part`.
- Thời điểm hoàn tất publish và pilot: `2026-08-12 17:30` (Asia/Bangkok).

- Chính sách cập nhật: `HASH_ONLY_LAN`; không tạo hoặc sử dụng khóa ký,
  `manifest.sig`, `key_id` hay `trusted_signing_keys`.
- Pilot đã nâng bản cài trên máy phát hành từ `0.1.1` lên `0.1.6` bằng chính
  luồng runtime updater: kiểm tra package, giải nén an toàn, xác minh manifest,
  health-check và kích hoạt đều đạt.
- `current.json` trỏ `0.1.6`; `previous.json` giữ `0.1.1` để rollback.
- Đã sao lưu và xác minh `21` file SQLite trong `backups/before-0.1.6`; dữ liệu
  runtime hiện hành khớp bản sao tại thời điểm cập nhật, không còn staging dở.
